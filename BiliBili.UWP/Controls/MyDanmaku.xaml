<UserControl
    x:Class="BiliBili.UWP.Controls.MyDanmaku"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BiliBili.UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    SizeChanged="UserControl_SizeChanged"
    >
    <Grid >
        
        <Grid x:Name="grid_Danmu">
            <Grid.RowDefinitions>
                <RowDefinition  x:Name="dan1"/>
                <RowDefinition  x:Name="dan2"/>
                <RowDefinition  x:Name="dan3"/>
                <RowDefinition  x:Name="dan4"/>
                <RowDefinition  x:Name="dan5"/>
                <RowDefinition  x:Name="dan6"/>
                <RowDefinition  x:Name="dan7"/>
                <RowDefinition  x:Name="dan8"/>
                <RowDefinition  x:Name="dan9"/>
                <RowDefinition  x:Name="dan10"/>
                <RowDefinition x:Name="Jianju" Height="0"/>
            </Grid.RowDefinitions>
            
        </Grid>
       
        <Grid x:Name="grid_Danmu2"  >
            <StackPanel x:Name="D_Top" VerticalAlignment="Top" />
            <StackPanel x:Name="D_Bottom" VerticalAlignment="Bottom"/>
        </Grid>
        <Grid Margin="0 -100 0 0" x:Name="d"></Grid>
    </Grid>
</UserControl>
